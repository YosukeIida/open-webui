# エージェント運用ガイド (Open WebUI)

## スコープと優先順位
- スコープ: 本ファイルが存在するディレクトリ配下に適用する.
- 優先順位: ユーザー指示 > 直近ディレクトリの `AGENTS.md` > リポジトリ直下の `AGENTS.md` > その他のドキュメント.
- 目的: エージェントが安全かつ一貫した方針で作業できる, 最小限の指針を与える.

## 応答言語
- 原則として日本語で応答する.
- ただし, ログ/CLI メッセージ/コード中の識別子や英語メッセージは英語でも良い.

## 作業一般ルール
- Python の実行を禁止する（例: `python`, `pytest`, `pip` など, Python インタプリタを起動するコマンド）.
- 場当たりの暫定対応を避け, 根本原因から修正し切る完全修正を原則とする.
- 必要に応じてスコープを広げ, 一貫性と整合性を確保する. 無関係な大規模リファクタは避ける.
- 互換性や影響範囲が大きい場合は, 移行計画（非推奨期間, feature flag, 段階導入）を明記する.
- 緊急時のみ短期 Hotfix を許容するが, 直後に完全修正の Issue/PR と期限を作成する.
- 既存のフォーマッタ/リンタ設定に従う. 整合性のために必要な範囲で実行する（差分の小ささより整合性を優先）.

## セットアップ & コマンド
- 前提: Docker Compose 上で `uv` の Python 環境を使用する.
- 依存の追加/更新: 依存は `pyproject.toml` を編集して追加する（`uv add` は使わない）.
  1. `pyproject.toml` の `[project]` または `[project.optional-dependencies]` を編集
  2. ロック更新: `./scripts/uv-lock.sh lock`
  3. 反映（インストール）: `./scripts/uv-lock.sh sync`
  - 注意: `docker-compose.webui-only.yaml` は prebuilt image を使うため, ロック更新だけでは実行中コンテナの依存は更新されない. 依存を実行環境に反映するにはカスタム image の build が必要.
- Lint/Format/Type-Check（導入済みのものを優先）:
  - Python Lint: `ruff check .`
  - Python Format: `ruff format .`
  - 型チェック（導入済みの場合）: `mypy .`

## コーディング規約
### 共通
- 一文字変数は避け, 意味のある命名をする.
- 例外処理は広すぎる `except Exception:` を避け, 失敗は可視化（Fail-fast）する.
- ミュータブルなデフォルト引数（`[]`, `{}`）は使用しない.
- 外部 I/O（ファイル/ネットワーク）は例外とタイムアウトを適切に扱い, 再試行は指数バックオフで行う（必要な場合のみ）.

### Python
- Python バージョンはリポジトリの `Dockerfile` / `pyproject.toml` に従う.
- `from __future__ import annotations` は使用しない.
- 型ヒントは極力付与する. `Optional[T]` は使わず `T | None` を使用する.
- コメントと docstring は日本語で書く. 句読点は ASCII の `,` と `.` を使用する.
- ログ出力は `print` を多用せず, プロジェクトで採用しているロギング手段に統一する（専用の logger ヘルパーがある場合はそれを優先）.
- 不要な依存の追加は禁止. 標準ライブラリや既存依存で実装することを優先する.
- パッケージ構成は必要に応じて PEP 420（implicit namespace packages）を採用してもよい（`__init__.py` は不要）.

## 設定・環境変数の扱い
- インフラ依存のパス/URL をハードコードしない. CLI 引数または環境変数で受け取る.
- 必須設定が未指定なら早期にエラーで終了する（曖昧に続行しない）.
- モデル名/データセット名などの仮固定値のハードコードは禁止. 入力・環境・プロバイダ推奨に基づき決定する.

## セキュリティとプライバシー
- 秘密情報は `.env`/環境変数からのみ取得する. ログ・例外メッセージに含めない.
- 外部ストレージや URL を扱う場合, 機微なクエリパラメータはマスクする.
- 収集データや生成物に個人情報が含まれる場合は, 必要最小限のみ保持する.

## テストと品質
- テストが存在する場合は, 変更に対応するユニットテストを追加する.
- Lint/Format/Type-Check はコミット前に通す（CI があれば同等のチェックに合格すること）.
- バグ再現時は回帰テストを先に追加してから修正する（テスト運用がある場合）.

## コミット/PR
- 目的・影響範囲・動作確認方法を PR 説明に簡潔に記す.
- コミットメッセージは要点が一読でわかる短文を推奨する（必要に応じて Conventional Commits を使用してもよい）.
